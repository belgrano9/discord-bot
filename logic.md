When a signal arrives:

1. **Signal arrives** → Webhook posts the approval message
2. **Bot monitors** → Watches for reactions on these specific messages
3. **You react ✅** → Bot springs into action
4. **Bot extracts data** → Parses the embed to get trading parameters
5. **Bot executes** → Uses your existing Binance order logic
